<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wafflestudio.snuev">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".SnuevApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".view.splash.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.main.MainActivity" />
        <activity android:name=".view.signin.SignInActivity" />
        <activity android:name=".view.signup.SignUpActivity" />
        <activity android:name=".view.signup.SignUpCompleteActivity" />
        <activity android:name=".view.search.SearchActivity" />
        <activity android:name=".view.detail.DetailActivity" />
        <activity
            android:name=".view.evaluate.EvaluateActivity"
            android:windowSoftInputMode="adjustPan|adjustResize" />
        <activity android:name=".view.profile.ProfileActivity" />
    </application>

</manifest>